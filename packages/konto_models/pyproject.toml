[project]
name = "konto-models"
version = "0.1.0"
dependencies = [
  "SQLAlchemy==2.0.32",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["konto_models*"]
