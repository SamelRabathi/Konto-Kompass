[project]
name = "konto-kompass-tests"
version = "0.1.0"
dependencies = [
  "pytest==8.3.4",
  "konto-connectors",
]

[tool.pytest.ini_options]
pythonpath = ["api", "worker", "packages/konto_connectors", "packages/konto_models"]
testpaths = ["tests"]
